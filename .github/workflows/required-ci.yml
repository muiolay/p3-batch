name: ci
on: [push, pull_request]
jobs:
  required-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "P3-BATCH-CI on ${{ github.ref }}"
          git log --oneline -n 8
          sleep 75
